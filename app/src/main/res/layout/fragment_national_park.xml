<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <!-- Park Image -->
    <ImageView
        android:id="@+id/park_image"
        android:layout_width="120dp"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

    <!-- Park Name -->
    <TextView
        android:id="@+id/park_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textAppearance="?attr/textAppearanceHeadline6"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/park_image"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Park Name" />

    <!-- Park Location -->
    <TextView
        android:id="@+id/park_location"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textAppearance="?attr/textAppearanceBody2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/park_name"
        app:layout_constraintTop_toBottomOf="@+id/park_name"
        tools:text="CA, NV" />

    <!-- Park Description -->
    <TextView
        android:id="@+id/park_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:textAppearance="?attr/textAppearanceBody2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/park_image"
        app:layout_constraintTop_toBottomOf="@+id/park_image"
        tools:text="This is a long description of the national park that will wrap to multiple lines and eventually be cut off with an ellipsis if it is too long to fit." />

</androidx.constraintlayout.widget.ConstraintLayout>

